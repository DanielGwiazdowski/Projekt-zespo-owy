<Window x:Class="czesci1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Logowanie" Height="350" Width="450" 
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#DDDDDD" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Nagłówek -->
            <Border Background="#0078D7" Grid.Row="0">
                <TextBlock Text="LOGOWANIE" Foreground="White" FontSize="20" 
                           FontWeight="Bold" Padding="20,15" HorizontalAlignment="Left"/>
            </Border>

            <!-- Zawartość -->
            <Border Background="White" Grid.Row="1" Padding="30,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Wprowadź swoje dane logowania" Grid.Row="0" Grid.ColumnSpan="2" 
                               FontSize="14" Margin="0,0,0,15"/>

                    <TextBlock Text="Nazwa użytkownika:" Grid.Row="1" Grid.Column="0" 
                               FontSize="14" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox x:Name="txtUsername" Grid.Row="1" Grid.Column="1"/>

                    <TextBlock Text="Hasło:" Grid.Row="2" Grid.Column="0" 
                               FontSize="14" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <PasswordBox x:Name="txtPassword" Grid.Row="2" Grid.Column="1"/>

                    <CheckBox x:Name="chkRemember" Content="Zapamiętaj mnie" 
                              Grid.Row="3" Grid.Column="1" Margin="0,10,0,0"/>

                    <StackPanel Grid.Row="4" Grid.Column="1" VerticalAlignment="Bottom">
                        <TextBlock Text="Nie masz konta? Skontaktuj się z administratorem."
                                   FontSize="12" Foreground="#666666" Margin="0,0,0,10"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Przyciski -->
            <Border Background="#F0F0F0" Grid.Row="2">
                <Grid Margin="20,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Anuluj" Grid.Column="0" HorizontalAlignment="Left" 
                            Width="100" Background="#E0E0E0" Foreground="Black"
                            Click="BtnCancel_Click"/>

                    <Button Content="Zaloguj" Grid.Column="1" HorizontalAlignment="Right"
                            Width="100" Background="#0078D7" Foreground="White"
                            Click="BtnLogin_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>